// Stub for expo/src/winter — prevents the native runtime from installing
// lazy globals (including __ExpoImportMetaRegistry) that fail in Jest's
// CommonJS environment when their getters fire outside test scope.
